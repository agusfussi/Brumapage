[build]
  command = "npx prisma db push --accept-data-loss && npm run build"
  publish = ".next"

[build.environment]
  SECRETS_SCAN_ENABLED = "false"
  SECRETS_SCAN_OMIT_PATHS = ".netlify/.next/cache"
  SECRETS_SCAN_OMIT_KEYS = "ADMIN_TOKEN"
